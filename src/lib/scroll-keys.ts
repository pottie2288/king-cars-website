/**
 * sessionStorage keys for restoring the showroom's scroll position and how many
 * cars were loaded, when a user returns from a car detail page.
 * Shared between ShowroomPage (writes/restores) and ScrollToTop (stands down).
 */
export const SHOWROOM_SCROLL_KEY = 'showroomScroll';
export const SHOWROOM_COUNT_KEY = 'showroomVisibleCount';
